# Use an official Ubuntu as a parent image
FROM ubuntu:latest

# Method 1: Using tail -f /dev/null
# Uncomment the following line to use this method
# CMD ["tail", "-f", "/dev/null"]

# Method 2: Using sleep infinity
# Uncomment the following line to use this method
# CMD ["sleep", "infinity"]

# Method 3: Using an infinite loop
# Uncomment the following line to use this method
# CMD ["sh", "-c", "while true; do sleep 1; done"]

# Method 4: Using a pseudo-TTY session
# This method is typically used with docker run command, not in Dockerfile
# CMD ["/bin/bash"]